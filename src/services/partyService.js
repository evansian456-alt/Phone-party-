'use strict';

/**
 * Party service.
 *
 * Business logic for party lifecycle (create, join, leave, end) will be
 * migrated here from server.js as part of the ongoing refactor.
 *
 * This file is a placeholder that documents the intended module boundary.
 * Implementations live in server.js until they can be safely extracted without
 * breaking the existing test suite.
 */

// TODO: migrate createParty, joinParty, leaveParty, endParty from server.js

module.exports = {};
